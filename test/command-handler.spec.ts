

describe("CommandHandler", function() {
  describe("#validate", function() {
    it("should return a boolean as to this command's validity", function() {
      assert.equal(false, true);
    });
  });
});
